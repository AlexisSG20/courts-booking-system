# Courts Booking System

Monorepo:
- `courts-api` (NestJS + Prisma + PostgreSQL)
- `courts-web` (React + Vite)

## Run (dev)

### Backend
cd courts-api
npm install
npm run start:dev

### Frontend
cd courts-web
npm install
npm run dev